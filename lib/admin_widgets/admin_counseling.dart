import 'package:flutter/material.dart';

class AdminCounseling extends StatelessWidget {
  const AdminCounseling({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
